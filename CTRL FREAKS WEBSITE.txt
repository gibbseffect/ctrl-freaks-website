<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CTRL Freaks | Official HQ</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    background: #0f0f1a;
    color: white;
    overflow-x: hidden;
}

canvas {
    position: fixed;
    top: 0;
    left: 0;
    z-index: -1;
}

h1, h2, h3 {
    font-family: 'Orbitron', sans-serif;
}

nav {
    position: fixed;
    width: 100%;
    background: rgba(0,0,0,0.6);
    backdrop-filter: blur(10px);
    padding: 15px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 1000;
}

nav a {
    color: white;
    text-decoration: none;
    margin-left: 20px;
    font-weight: 600;
    transition: 0.3s;
}

nav a:hover {
    color: #00f7ff;
}

.hero {
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 20px;
}

.hero h1 {
    font-size: 4rem;
    background: linear-gradient(90deg, #00f7ff, #a855f7);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.hero p {
    margin-top: 20px;
    font-size: 1.2rem;
    opacity: 0.8;
}

.btn {
    margin-top: 30px;
    padding: 12px 28px;
    background: linear-gradient(90deg, #00f7ff, #a855f7);
    border: none;
    border-radius: 30px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
    color: black;
}

.btn:hover {
    transform: scale(1.05);
    box-shadow: 0 0 20px #00f7ff;
}

section {
    padding: 100px 20px;
    max-width: 1200px;
    margin: auto;
}

.section-title {
    text-align: center;
    margin-bottom: 60px;
    font-size: 2.5rem;
}

.schedule-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
}

.card {
    background: rgba(255,255,255,0.05);
    padding: 20px;
    border-radius: 15px;
    transition: 0.3s;
    border: 1px solid rgba(255,255,255,0.1);
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 0 20px rgba(0,247,255,0.4);
}

.countdown {
    text-align: center;
    font-size: 1.5rem;
    margin-top: 20px;
    color: #00f7ff;
}

footer {
    text-align: center;
    padding: 40px 20px;
    background: rgba(0,0,0,0.6);
    margin-top: 50px;
}
</style>
</head>

<body>

<canvas id="particles"></canvas>

<nav>
    <h3>CTRL Freaks</h3>
    <div>
        <a href="#schedule">Schedule</a>
        <a href="#countdownSection">Next Event</a>
        <a href="#join">Join</a>
    </div>
</nav>

<section class="hero">
    <h1>CTRL Freaks</h1>
    <p>Gaming. Community. Chaos.</p>
    <button class="btn" onclick="scrollToJoin()">Join The Discord</button>
</section>

<section id="countdownSection">
    <h2 class="section-title">Next Event Countdown</h2>
    <div class="countdown" id="countdown">
        Loading countdown...
    </div>
</section>

<section id="schedule">
    <h2 class="section-title">Weekly Schedule</h2>
    <div class="schedule-grid">
        <div class="card"><h3>Monday</h3><p>🧙 Salem Night</p></div>
        <div class="card"><h3>Tuesday</h3><p>🎮 Game Night</p></div>
        <div class="card"><h3>Wednesday</h3><p>🎬 Movie Night</p></div>
        <div class="card"><h3>Thursday</h3><p>🐉 DnD Night</p></div>
        <div class="card"><h3>Friday</h3><p>🍻 Club / Bar Night</p></div>
        <div class="card"><h3>Saturday</h3><p>💬 Discord Hangout</p></div>
        <div class="card"><h3>Sunday</h3><p>🛋 Chill Day</p></div>
    </div>
</section>

<section id="join">
    <h2 class="section-title">Join CTRL Freaks</h2>
    <div style="text-align:center;">
        <p>Become part of the community. Jump in. Stay chaotic.</p>
        <button class="btn">Discord Invite Link</button>
    </div>
</section>

<footer>
    <p>© 2026 CTRL Freaks | Built for the Community</p>
</footer>

<script>
/* Smooth Scroll */
function scrollToJoin(){
    document.getElementById("join").scrollIntoView({behavior:"smooth"});
}

/* Countdown Timer */
const eventDate = new Date();
eventDate.setDate(eventDate.getDate() + 2); // Change this to your real event date

function updateCountdown() {
    const now = new Date();
    const diff = eventDate - now;

    if (diff <= 0) {
        document.getElementById("countdown").innerHTML = "🚀 LIVE NOW!";
        return;
    }

    const days = Math.floor(diff / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diff / (1000 * 60 * 60)) % 24);
    const minutes = Math.floor((diff / 1000 / 60) % 60);
    const seconds = Math.floor((diff / 1000) % 60);

    document.getElementById("countdown").innerHTML =
        `${days}d ${hours}h ${minutes}m ${seconds}s`;
}

setInterval(updateCountdown, 1000);
updateCountdown();

/* Animated Background Particles */
const canvas = document.getElementById("particles");
const ctx = canvas.getContext("2d");

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let particlesArray = [];

class Particle {
    constructor() {
        this.x = Math.random() * canvas.width;
        this.y = Math.random() * canvas.height;
        this.size = Math.random() * 2 + 1;
        this.speedX = Math.random() * 1 - 0.5;
        this.speedY = Math.random() * 1 - 0.5;
    }
    update() {
        this.x += this.speedX;
        this.y += this.speedY;
    }
    draw() {
        ctx.fillStyle = "#00f7ff";
        ctx.beginPath();
        ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
        ctx.fill();
    }
}

function init() {
    for (let i = 0; i < 100; i++) {
        particlesArray.push(new Particle());
    }
}

function animate() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    particlesArray.forEach(p => {
        p.update();
        p.draw();
    });
    requestAnimationFrame(animate);
}

init();
animate();

window.addEventListener("resize", () => {
    canvas.width = window.innerWidth;
    canvas.height = window.innerHeight;
});
</script>

</body>
</html>